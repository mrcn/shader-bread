import React from "react";
import "../styles.css";

const Navigation = () => {
  return (
    <nav>
      <a href="#" className="logo">
        one with ux
      </a>
      <a href="#">home</a>
      <a href="#">about</a>
      <a href="#">portfolio</a>
      <a href="#">rambling</a>
      <a href="#">contact</a>
    </nav>
  );
};

export default Navigation;